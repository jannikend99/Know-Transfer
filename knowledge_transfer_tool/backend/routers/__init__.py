from . import processes # Add other routers here as they are created

# This could be a list or dict of routers if you have many
# For now, just importing to make them available
api_router = processes.router # Directly assign if only one for now, or combine later 